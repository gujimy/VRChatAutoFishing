

添加随机蓄力时间选项，勾选启用，使用拖动调节蓄力时间随机蓄力时间最大值，蓄力时间最小值固定0.2 ✔


1.添加快捷键 F5 启动/停止 F6 强制停止 ✔

2.添加任务栏图标 ✔
任务栏图标 颜色按状态切换颜色，未启动时为灰色，启动时 颜色按 钓鱼状态切换颜色，




备选BUG修复：

中途手动操作了游戏 有可能卡在收杆中 分析问题

检查超时处理逻辑，有没有可能会在其他状态中超时

其他状态中也添加超时处理，防止程序卡在某个状态
可以为每个状态设置独立的超时时间，更精细地控制程序行为
检查 收杆中 是否 会超时，怎么解决 "收杆中"状态确实存在超时风险